[project]
name = "hr-interview-evaluator"
version = "0.1.0"
description = "HR Interview Evaluator Agent"

dependencies = [
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0",
    "openai"
]

[project.scripts]
server = "server.app:main"
